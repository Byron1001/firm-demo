export const siteSettingsQuery = `*[_type == "siteSettings"][0]`

export const practiceAreasQuery = `*[_type == "practiceArea"] | order(order asc)`

export const teamMembersQuery = `*[_type == "teamMember"] | order(order asc)`

export const officesQuery = `*[_type == "office"] | order(order asc)`

export const testimonialsQuery = `*[_type == "testimonial"] | order(order asc)`

export const galleryClientsQuery = `*[_type == "galleryClient"] | order(order asc)`

export const faqsQuery = `*[_type == "faq"] | order(order asc)`

export const careersQuery = `*[_type == "career"] | order(order asc)`


